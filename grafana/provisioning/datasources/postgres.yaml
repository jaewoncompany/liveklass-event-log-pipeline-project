apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: postgres
    url: db:5432
    user: postgres
    secureJsonData:
      password: ${GF_DB_PASSWORD}
    jsonData:
      database: eventlog
      sslmode: disable
      postgresVersion: 1600
    isDefault: true
