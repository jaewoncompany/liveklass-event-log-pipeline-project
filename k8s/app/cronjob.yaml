apiVersion: batch/v1
kind: CronJob
metadata:
  name: event-generator-cronjob
  namespace: event-pipeline
spec:
  schedule: "0 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: event-generator
              image: event-generator:latest
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: event-pipeline-config
              env:
                - name: DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: event-pipeline-secret
                      key: DB_PASSWORD
              resources:
                requests:
                  memory: "256Mi"
                  cpu: "250m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
              volumeMounts:
                - name: output
                  mountPath: /output
          volumes:
            - name: output
              emptyDir: {}
